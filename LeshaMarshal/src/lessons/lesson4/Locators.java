package lesson4;

import org.example.Main;
import org.openqa.selenium.By;
import org.openqa.selenium.WebElement;

public class Locators  {


/*
    WebElement input  = driver = diver.findElement(By.XPath("//input[@aria-label='Найти']"))  - принимает в себя один аргумент
    WebElement input  = driver = diver.findElements(By.XPath("//input[@aria-label='Найти']")) - несколько

    WebElement input  = driver = diver.findElement(By.cssSelector())
    WebElement input  = driver = diver.findElement(By.className())
    WebElement input  = driver = diver.findElement(By.id())
    WebElement input  = driver = diver.findElement(By.linkText())
    WebElement input  = driver = diver.findElement(By.name())
    WebElement input  = driver = diver.findElement(By.tagName())

*/

}

